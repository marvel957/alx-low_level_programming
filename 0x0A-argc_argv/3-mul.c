#include <stdio.h>
#include <stdlib.h>
/**
 * main - fumc name
 * @argc: arg counter
 * @argv: argument variable
 * Return:a value
 */
int main(int argc, char *argv[])
{
	if (argc != 3)
	{
		printf("Error\n");
		return (1);
	}
	printf("%d\n", atoi(argv[1]);
	return (0);
}
